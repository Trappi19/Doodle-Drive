<ui:FluentWindow x:Class="DoodleDrive.Views.Dialogs.UserEditorDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Compte utilisateur"
                 Width="420" SizeToContent="Height"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="Bar" Title="Compte utilisateur" ShowMaximize="False" ShowMinimize="False" />

        <StackPanel Grid.Row="1" Margin="24,12,24,8">
            <TextBlock x:Name="HeaderText" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />

            <StackPanel x:Name="UsernamePanel">
                <TextBlock Text="Identifiant" FontWeight="SemiBold" Margin="2,0,0,4" />
                <ui:TextBox x:Name="UsernameBox" PlaceholderText="Nom d'utilisateur" />
            </StackPanel>

            <TextBlock Text="Mot de passe" FontWeight="SemiBold" Margin="2,14,0,4" />
            <PasswordBox x:Name="PasswordBox" MinHeight="38" />

            <StackPanel x:Name="RolePanel">
                <TextBlock Text="Rôle" FontWeight="SemiBold" Margin="2,14,0,4" />
                <ComboBox x:Name="RoleBox" MinHeight="38">
                    <ComboBoxItem Content="Utilisateur" IsSelected="True" />
                    <ComboBoxItem Content="Administrateur" />
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,12,24,20">
            <ui:Button Content="Annuler" Appearance="Secondary" IsCancel="True"
                       MinWidth="96" Margin="0,0,10,0" Click="Cancel_OnClick" />
            <ui:Button Content="Valider" Appearance="Primary" IsDefault="True"
                       MinWidth="96" Click="Ok_OnClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
