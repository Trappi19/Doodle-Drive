<ui:FluentWindow x:Class="DoodleDrive.Views.Dialogs.RemoteFolderPickerDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:models="clr-namespace:DoodleDrive.Models"
                 xmlns:vm="clr-namespace:DoodleDrive.ViewModels"
                 mc:Ignorable="d"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 d:DataContext="{d:DesignInstance vm:RemoteFolderPickerViewModel}"
                 Title="Choisir un emplacement en ligne"
                 Width="460" Height="560"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowCornerPreference="Round"
                 ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Emplacement en ligne" ShowMaximize="False" ShowMinimize="False" />

        <TextBlock Grid.Row="1" Margin="18,4,18,10" Opacity="0.7" TextWrapping="Wrap"
                   Text="Choisissez le dossier du drive où placer la synchronisation." />

        <Border Grid.Row="2" Margin="18,0,18,0" CornerRadius="8"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
            <Grid>
                <TreeView x:Name="Tree" ItemsSource="{Binding Roots}"
                          Background="Transparent" BorderThickness="0" Padding="6"
                          SelectedItemChanged="Tree_OnSelectedItemChanged">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type models:FolderNode}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <TextBlock Style="{StaticResource GlyphText}" Text="&#xE8B7;" FontSize="15"
                                           Foreground="{StaticResource KindFolderBrush}" Margin="0,0,8,0" />
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                </TreeView>

                <ui:ProgressRing IsIndeterminate="True" Width="34" Height="34"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
            </Grid>
        </Border>

        <CheckBox Grid.Row="3" Margin="18,12,18,0" IsChecked="{Binding CreateSubfolder, Mode=TwoWay}">
            <TextBlock TextWrapping="Wrap">
                <Run Text="Créer un sous-dossier «" />
                <Run Text="{Binding LocalFolderName, Mode=OneWay}" FontWeight="SemiBold" />
                <Run Text="» (recommandé — comme envoyer le dossier)" />
            </TextBlock>
        </CheckBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="18,16,18,16">
            <ui:Button Content="Annuler" MinWidth="90" Margin="0,0,8,0" Click="Cancel_OnClick" />
            <ui:Button Content="Choisir" Appearance="Primary" MinWidth="90" Click="Ok_OnClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
