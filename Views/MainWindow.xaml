<ui:FluentWindow x:Class="DoodleDrive.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:vm="clr-namespace:DoodleDrive.ViewModels"
                 xmlns:svc="clr-namespace:DoodleDrive.Services"
                 mc:Ignorable="d"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 d:DataContext="{d:DesignInstance vm:ShellViewModel}"
                 Title="Doodle Drive"
                 Width="1180" Height="760"
                 MinWidth="900" MinHeight="560"
                 WindowStartupLocation="CenterScreen"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 Icon="pack://application:,,,/assets/icon.ico">

    <ui:FluentWindow.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="10,2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="8" Background="Transparent" Padding="12,0">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                            </Trigger>
                            <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Doodle Drive">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/assets/icon.ico" Width="18" Height="18" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- ==================== Rail de navigation ==================== -->
            <Border Grid.Column="0" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,10,0,0">
                        <Button Style="{StaticResource NavButton}"
                                Command="{Binding NavigateFilesCommand}"
                                Tag="{Binding IsFilesActive}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource GlyphText}" Text="&#xE8B7;" FontSize="18" Width="28" />
                                <TextBlock Text="Mes fichiers" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButton}"
                                Command="{Binding NavigateAdminCommand}"
                                Tag="{Binding IsAdminActive}"
                                Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource GlyphText}" Text="&#xE7EF;" FontSize="18" Width="28" />
                                <TextBlock Text="Administration" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButton}"
                                Command="{Binding NavigateSettingsCommand}"
                                Tag="{Binding IsSettingsActive}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource GlyphText}" Text="&#xE713;" FontSize="18" Width="28" />
                                <TextBlock Text="Paramètres" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Profil + déconnexion -->
                    <Border Grid.Row="1" Padding="12,12" Margin="10,8,10,12" CornerRadius="10"
                            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="38" Height="38" CornerRadius="19"
                                    Background="{DynamicResource AccentFillColorDefaultBrush}">
                                <TextBlock Text="{Binding Initials}" Foreground="White"
                                           FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding UserName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding RoleLabel}" FontSize="11" Opacity="0.7" />
                            </StackPanel>
                            <ui:Button Grid.Column="2" Command="{Binding SignOutCommand}"
                                       Appearance="Secondary" Padding="8" ToolTip="Se déconnecter">
                                <TextBlock Style="{StaticResource GlyphText}" Text="&#xE7E8;" FontSize="16" />
                            </ui:Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- ==================== Contenu ==================== -->
            <Grid Grid.Column="1">
                <ContentControl Content="{Binding CurrentPage}"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch" />

                <!-- ==================== Toasts ==================== -->
                <ItemsControl ItemsSource="{Binding Toasts}"
                              HorizontalAlignment="Right" VerticalAlignment="Bottom"
                              Margin="0,0,20,20" Width="340" IsHitTestVisible="True">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type svc:Toast}">
                            <Border Margin="0,8,0,0" CornerRadius="10" Padding="0"
                                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.25" />
                                </Border.Effect>
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="Slide" X="0" />
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="Border.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                 From="0" To="1" Duration="0:0:0.25" />
                                                <DoubleAnimation Storyboard.TargetName="Slide"
                                                                 Storyboard.TargetProperty="X"
                                                                 From="60" To="0" Duration="0:0:0.28">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" CornerRadius="10,0,0,10"
                                            Background="{Binding AccentBrushKey, Converter={StaticResource KeyToBrush}}" />
                                    <TextBlock Grid.Column="1" Style="{StaticResource GlyphText}"
                                               Text="{Binding Glyph}" FontSize="20" Margin="12,0,0,0"
                                               Foreground="{Binding AccentBrushKey, Converter={StaticResource KeyToBrush}}" />
                                    <StackPanel Grid.Column="2" Margin="12,10">
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Message}" Opacity="0.75" TextWrapping="Wrap"
                                                   FontSize="12" Margin="0,2,0,0"
                                                   Visibility="{Binding Message, Converter={StaticResource StringToVis}}" />
                                    </StackPanel>
                                    <Button Grid.Column="3" VerticalAlignment="Top" Margin="0,6,6,0"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Command="{Binding DataContext.DismissToastCommand,
                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}">
                                        <TextBlock Style="{StaticResource GlyphText}" Text="&#xE711;" FontSize="12" Opacity="0.6" />
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
